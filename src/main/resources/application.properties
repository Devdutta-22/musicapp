# ========================================
# App Name
# ========================================
spring.application.name=musicapp

# ========================================
# Database Configuration (uses ENV variables)
# ========================================
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://turntable.proxy.rlwy.net:32938/railway?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:VUZaeNtjZHpAzJkONXFePRBAACdXhBrG}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# --- CHANGE START ---
# 1. Define the R2 URL using the name your Java code expects (FILES_BASE_URL)
FILES_BASE_URL=https://pub-2798f4c196da403cbeb5ae2b60ccc805.r2.dev

# 2. Update these to reference the variable above (So they are all synced)
app.media.base-path=${FILES_BASE_URL}
storage.base-url=${FILES_BASE_URL}
# --- CHANGE END ---

#gemini key
# Read the list of API keys (Comma separated)
gemini.api.keys=${GEMINI_API_KEYS}

# ========================================
# Hibernate / JPA
# ========================================
spring.jpa.hibernate.ddl-auto=update
# Let Hibernate autodetect dialect from JDBC metadata (avoid explicit class)
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=true

# ========================================
# Multipart / File Upload Settings
# ========================================
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=200MB
spring.servlet.multipart.max-request-size=200MB

# ========================================
# Server Port (Render uses PORT env var)
# ========================================
server.port=${PORT:8080}

# ========================================
# CORS (optional - you will add WebConfig.java soon)
# ========================================
